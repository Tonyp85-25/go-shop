pre-commit:
  commands:
    "Lint reminder":
      run: echo "Do not forget to lint the code before committing && exit 0"

commit-msg:
  commands:
    "Check commit formatting":
      run: devbox run commitlint --from HEAD~1 --to HEAD
