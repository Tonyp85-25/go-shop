pre-commit:
  jobs:
    - name: Lint reminder
    - run: echo "Do not forget to lint the code before committing"

commit-msg:
  jobs:
    - Check commit formatting
    - run: commitlint --from HEAD~1 --to HEAD --verbose